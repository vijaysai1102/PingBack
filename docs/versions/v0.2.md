# PingBack v0.2 — Multi-Agent Attention Layer

## 1. Goals & Summary

PingBack v0.2 evolves PingBack from a Claude-specific attention notifier into a unified local multi-agent attention layer supporting **Claude Code**, **Codex CLI**, and **AGY CLI (Antigravity CLI)** on both **Windows** and **macOS**.

Key capabilities introduced in v0.2:

- Multi-agent adapter architecture (`claude`, `codex`, `agy`).
- Multi-agent session tracking, event routing, and enhanced `pingback status` display.
- Cross-platform terminal focusing ("Return to Agent" notification actions).
- Granular notification & audio customization via configuration and CLI (`pingback config`).
- Strict preservation of v0.1 invariants: 100% local-only, zero-config default, platform isolation, and non-blocking failure isolation.

---

## 2. Scope & Requirements

### Feature 1: Codex CLI Integration

- [x] Research and detect installed Codex CLI and version.
- [x] Setup & uninstall lifecycle hooks for Codex integration.
- [x] Normalize Codex events into PingBack's common `AgentEvent` model (`attention_required`, `question`, `error`, `task_completed`).
- [x] Session tracking, PID, and working directory identification.
- [x] Graceful fallback if Codex is not installed or errors occur.

### Feature 2: AGY CLI Integration

- [x] Research and detect installed AGY / Antigravity CLI and version.
- [x] Setup & uninstall integration using official AGY customization/hook mechanisms.
- [x] Normalize AGY events into PingBack's common `AgentEvent` model.
- [x] Track AGY sessions, PIDs, cwd, and attention states.
- [x] Graceful fallback if AGY is not installed.

### Feature 3: Multi-Agent Session & Status Handling

- [x] Extend `AgentType` union (`claude` | `codex` | `agy`) and `AgentSession` model.
- [x] Handle simultaneous sessions across multiple agents without collision or focus leakage.
- [x] Update `pingback setup` to scan all supported agents and configure available ones gracefully.
- [x] Enhance `pingback status` CLI to display active sessions, attention needs, and waiting times grouped by agent.

### Feature 4: Cross-Platform Terminal Focusing

- [ ] Platform-independent `TerminalFocusService` interface.
- [ ] Windows terminal focusing (`src/platform/windows/`): PID to window hierarchy detection, window foregrounding, fallback handling.
- [ ] macOS terminal focusing (`src/platform/macos/`): AppleScript/process mapping and foregrounding.
- [ ] Notification action integration: "Return to Agent" action dispatch linked to specific `sessionId`.
- [ ] Graceful fallback when terminal cannot be focused without breaking notifications.

### Feature 5: Notification & Sound Customization

- [ ] Desktop notifications toggle (`ON`/`OFF`).
- [ ] Sound toggle (`ON`/`OFF`) and volume control (`0.0` - `1.0`).
- [ ] Event-level notification/sound rules (`attention_required`, `question`, `error`, `task_completed`).
- [ ] Backward-compatible configuration schema (preserves existing v0.1 user configs).
- [ ] Enhanced `pingback config` command to view and update preferences.

---

## 3. Technical Plan

### Module & Directory Structure

```text
src/
├── agents/
│   ├── adapter.ts           # Core AgentAdapter interface & registry
│   ├── claude/              # Existing Claude adapter & hooks
│   ├── codex/               # Codex adapter & event normalizer
│   └── agy/                 # AGY adapter & event normalizer
├── core/
│   ├── event-types.ts       # Extended AgentType, AgentEvent definitions
│   ├── daemon.ts            # Daemon lifecycle, multi-agent event routing
│   └── router.ts            # Attention routing & dispatch
├── sessions/
│   ├── session-types.ts     # Extended AgentSession schema (terminal info, status)
│   └── session-store.ts     # Multi-agent session persistence & isolation
├── notifications/
│   ├── notification-types.ts
│   ├── notification-policy.ts # Event-level customization rules
│   └── notifier.ts          # Desktop toast dispatch with actions
├── platform/
│   ├── platform-interface.ts
│   ├── windows/             # Windows toast, audio, and window focusing
│   └── macos/               # macOS toast, audio, and window focusing
└── cli/
    ├── commands/
    │   ├── setup.ts         # Multi-agent detection & setup
    │   ├── status.ts        # Multi-agent grouped status output
    │   └── config.ts        # Notification preference management
```

### Architectural Decisions & Invariants

1. **Agent-Agnostic Core**: Daemon, router, sessions, and notification engine operate exclusively on normalized models.
2. **Platform Abstraction**: All OS-specific code (Windows PowerShell/Win32 APIs vs macOS osascript/AppleScript) stays behind platform interfaces.
3. **Failure Isolation**: An unavailable or broken agent integration never degrades other active agent adapters or core notifications.

---

## 4. Continuous Work Log

### Baseline Verification (Milestone 1)

- [x] Initialized v0.2 specification and roadmap in `docs/versions/v0.2.md`.
- [x] Verified baseline unit & integration test suite (24 suites, 314 tests passed).

### Feature 1: Codex CLI Integration (Milestone 2)

- [x] Researched Codex CLI environment (`codex-cli 0.147.0`, `~/.codex/hooks.json`, `~/.codex/config.toml`).
- [x] Extended `AgentType` in `src/core/types.ts` to include `'codex' | 'agy'`.
- [x] Implemented `src/agents/codex/types.ts` defining `CodexHookPayload`, `CodexHookEvent`, and `isCodexHookEvent`.
- [x] Implemented `src/agents/codex/detector.ts` for PATH launcher detection and `~/.codex` home discovery.
- [x] Implemented `src/agents/codex/normalize.ts` normalizing `UserPromptSubmit` to working session state and `Stop` to `attention_required` or `error`.
- [x] Implemented `src/agents/codex/settings.ts` managing `hooks.json` installation with non-destructive merge, atomic backup (`hooks.json.pingback-backup`), and `computeHookHash`.
- [x] Implemented `src/agents/codex/hook-entry.ts` standalone bridge forwarding JSON payloads over IPC to the daemon without throwing errors.
- [x] Implemented `src/agents/codex/adapter.ts` implementing the `AgentAdapter` contract.
- [x] Created comprehensive unit test suites in `tests/agents/codex/`:
  - `detector.test.ts` (4 tests)
  - `normalize.test.ts` (6 tests)
  - `settings.test.ts` (7 tests)
  - `adapter.test.ts` (5 tests)
- [x] Verified all 28 test suites (341 tests) pass, typecheck passes, lint passes, and build passes.
- [x] Committed and pushed Feature 1 (`feat(codex): add Codex CLI integration and event normalizer`) to GitHub.

### Feature 2: AGY CLI Integration (Milestone 3)

- [x] Researched Google Antigravity / AGY CLI environment (`agy 1.1.15`, `~/.gemini/config/hooks.json`, official customization docs).
- [x] Implemented `src/agents/agy/types.ts` defining `AGYHookPayload`, `AGYHookEvent`, `AGYToolCall`, and `isAGYHookEvent`.
- [x] Implemented `src/agents/agy/detector.ts` for multi-alias executable scanning (`agy`, `antigravity`, `gemini`) and configuration root locator (`~/.gemini`, `~/.agy`, `~/.antigravity`).
- [x] Implemented `src/agents/agy/normalize.ts` normalizing `PreInvocation` to working state, `PreToolUse(ask_question)` to question event, and `Stop` to `attention_required` or `error`.
- [x] Implemented `src/agents/agy/settings.ts` managing `hooks.json` under `"pingback"` named hook block with non-destructive merge and atomic backup.
- [x] Implemented `src/agents/agy/hook-entry.ts` standalone bridge fulfilling AGY's `{}` stdout contract and forwarding events over IPC.
- [x] Implemented `src/agents/agy/adapter.ts` implementing the `AgentAdapter` contract.
- [x] Created comprehensive unit test suites in `tests/agents/agy/`:
  - `detector.test.ts` (4 tests)
  - `normalize.test.ts` (5 tests)
  - `settings.test.ts` (4 tests)
  - `adapter.test.ts` (5 tests)
- [x] Verified all 32 test suites (364 tests) pass, typecheck passes, lint passes, and build passes.
- [x] Committed and pushed Feature 2 (`feat(agy): add AGY CLI integration and event normalizer`) to GitHub.

### Feature 3: Multi-Agent Session & Status Handling (Milestone 4)

- [x] Implemented `src/agents/registry.ts` creating unified agent discovery and adapter registry (`createAllAdapters`, `getAdapter`).
- [x] Extended `src/core/event-schema.ts` to validate and parse events/session updates from all supported agents (`claude`, `codex`, `agy`).
- [x] Updated `src/sessions/session-manager.ts` to index sessions by composite key `(agent, sessionId)` guaranteeing collision-free simultaneous sessions across agents.
- [x] Updated `src/core/ipc/protocol.ts` and `src/core/daemon.ts` to report multi-agent configuration and health in `DaemonStatus`.
- [x] Updated `src/cli/commands/setup.ts` to discover and configure all detected agents gracefully, requiring at least one agent.
- [x] Updated `src/cli/commands/setup.ts` (`runUninstall`) to remove hooks cleanly across all configured adapters.
- [x] Enhanced `src/cli/status-view.ts` to render agent configuration states and active sessions grouped with agent names, project directories, running/waiting durations, and attention counts.
- [x] Created `tests/agents/registry.test.ts` and updated session/status test suites.
- [x] Verified all 33 test suites (368 tests) pass, typecheck passes, lint passes, and build passes.
