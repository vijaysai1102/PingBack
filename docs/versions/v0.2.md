# PingBack v0.2

## Goals & Summary

Make the existing Claude Code experience more dependable and configurable while preserving the local-only v0.1 architecture. v0.2 adds validated hierarchical notification settings, event-aware notification timing and sound behavior, stronger multi-session project association, and safe IDE/application focusing after a notification action.

## Scope & Requirements

- Preserve existing Windows and macOS support, Claude Code integration, local daemon, native notifications, sounds, and session tracking.
- Add backward-compatible hierarchical configuration for notification enablement, sound enablement and volume, plus per-event enablement and delay.
- Use five-second default delays for all configured events: attention required, question, turn completion, error, and task completed. Preserve any explicitly configured non-negative delay rather than rewriting it during load.
- Validate configuration values and support `pingback config set <path> <value>` and `pingback config get <path>` for nested values.
- Produce concise native notifications that identify Claude Code, the event, and the associated project where available; honor sound settings without adding unnecessary audio dependencies.
- Improve event classification and ensure multiple Claude sessions retain their own project/session association.
- Add a platform-independent application-focus abstraction with Windows and macOS implementations. Detect and focus only a reliably associated IDE/application; notification delivery must succeed even when focus fails.
- Add a notification action, where the platform supports it, to return to the associated project. Do not focus a specific terminal or terminal tab.
- Improve setup and status output, including practical IDE discovery and concise multi-session/waiting summaries.
- Add coverage for configuration, event delays/sound, session association, application detection/focus, platform boundaries, and backward compatibility.
- Update README with v0.2 behavior, configuration, focusing limitations, privacy, and troubleshooting.

### Out of Scope

- No new agent integrations, Linux support, remote/cloud services, telemetry, mobile/web apps, third-party notification channels, or terminal-specific focusing.

## Technical Plan

### Audit result and preserved behavior

- `src/agents/claude/` uses Claude Code hooks and converts them to the core event model. The hook bridge has a short timeout and must remain non-blocking.
- `src/core/` validates IPC input, deduplicates events, routes them to `SessionManager`, and contains no Claude-specific logic.
- `src/sessions/` already keys state by Claude session ID, so concurrent sessions do not overwrite one another. v0.2 will extend this rather than introduce a global current session.
- `src/notifications/` formats native desktop notifications and plays generated WAV tones; it currently uses a flat desktop/sound config and immediately awaits delivery.
- `src/platform/` contains the Windows/macOS boundary for paths, IPC, and audio. Application discovery and focusing will join this boundary rather than add scattered operating-system checks.
- `src/cli/` already provides setup, status, and flat configuration commands. It will be extended in place.

### Feature implementation plan

1. **Backward-compatible hierarchical configuration**
   - Extend `src/config/config-manager.ts` to normalize the v0.2 notification tree: global enablement, sound enablement/volume, and per-event enablement/delay.
   - Read v0.1 `notifications.desktop` and boolean `notifications.sound` values as legacy inputs, migrate in memory to the new form, and retain safe defaults for absent or malformed settings.
   - Validate each write before persistence: booleans, a finite sound volume from 0 through 1, and non-negative finite delays. Invalid values will produce a focused CLI error and leave the current file unchanged.
   - Default every configured event (attention required, question, turn completion, error, and task completed) to 5 seconds. Continue to honor any explicitly saved non-negative event delay.

2. **Hierarchical configuration CLI**
   - Extend `src/cli/index.ts` and `src/cli/commands/config.ts` with `pingback config get <path>` plus nested `set` paths.
   - Keep `pingback config` as a concise full configuration view. `get notifications` will render the notification subtree, while `set` will support the documented sound and event-delay examples.
   - Support the existing flat key inputs as compatibility aliases where they do not conflict with the hierarchical schema, and show users the canonical v0.2 path.

3. **Event-aware notification scheduling**
   - Add a small, injectable scheduler near the notification policy/daemon boundary. It will accept the hook event immediately, schedule its notification for the configured delay, and keep the hook's fast IPC acknowledgement intact.
   - Key pending work by session and event type, cancel a stale pending attention notification when that session returns to `working` or ends, and preserve existing duplicate suppression before scheduling.
   - Keep routing/session persistence synchronous; only the user-facing delivery is delayed. This avoids five-second hook timeouts and stale notifications after a developer has already responded.
   - Confirm the final Claude notification mapping against current official Claude Code hook documentation before changing it. In particular, distinguish blocking permission/input signals, actual questions, normal turn completion, task completion, and failures rather than inferring from terminal output.

4. **Notification content and sound behavior**
   - Extend `src/notifications/notification-policy.ts` so notification enablement, event enablement, delay, project label, sound enabled state, and volume flow into a typed request.
   - Update `src/notifications/desktop-notification.ts` and `src/notifications/sound-service.ts` so every enabled v0.2 event receives a concise native notification and, by default, a short tone. Global sound disablement suppresses playback without suppressing the toast.
   - Pass configured volume through the existing platform sound abstraction using native capabilities only; no audio framework will be introduced. Event-specific sound overrides will be included only if they fit the final normalized schema without complicating the CLI.

5. **Safe IDE/application association and focusing**
   - Add a platform-independent application-focus contract (for application discovery, project-path association, and foregrounding) under a new `src/applications/` boundary, wired through the existing platform factory.
   - Add Windows and macOS implementations behind that contract. They will inspect available development applications only when needed, use a session's `cwd` to form a project association, and return no match when the association is not reliable.
   - Prioritize VS Code and Cursor as editors to foreground, not as additional coding-agent integrations. Other editors will be added only where detection is safe and testable.
   - Attach a resolved application association to the individual session (or safe session metadata) so simultaneous Claude sessions retain separate project targets. The association will be refreshed on meaningful session activity, not by aggressive background polling.
   - Keep focus best-effort: a failed or unsupported focus request is logged and never prevents notification delivery, never selects an unrelated app, and never focuses a terminal/tab.

6. **Notification activation**
   - Extend the notification request/service contract with a session-bound activation callback or identifier. Where the native notifier can report a click/action, it will invoke the focus service for that same session's associated application.
   - Validate node-notifier's current Windows/macOS action and click behavior before implementation. If a platform cannot expose a reliable action, clicking remains a harmless dismissal rather than guessing a target.

7. **Session, status, and setup improvements**
   - Preserve session-ID identity in `SessionManager`; extend its persisted model only with the minimal project/application association needed for focusing.
   - Refine `src/cli/status-view.ts` to show concise per-project multi-session state, waiting duration, active-session total, and attention total.
   - Extend `src/cli/commands/setup.ts` to report notification availability, sound availability, installed Claude integration, and non-fatal detection of supported editors. PingBack remains fully usable when no editor is found.

8. **Documentation, versioning, and tests**
   - Bump package/release documentation to v0.2 when implementation begins, and update `README.md` with the configuration tree, defaults, delays, focusing behavior, supported editors, privacy, troubleshooting, and terminal-focusing limitation.
   - Extend the existing Vitest suite rather than replace it: config parsing/migration/validation/persistence; scheduler delay/cancellation; notification/sound behavior; concurrent session-to-project association; platform focus detection/success/failure; CLI get/set/status/setup; and end-to-end event-to-notification behavior.
   - Run unit/integration checks after each major step. Perform real Windows and macOS notification/focus validation separately; automated cross-platform tests will mock platform commands but will not be presented as proof of native foregrounding.

### Expected module changes

| Area                           | Existing files to extend                                                                                                      | Likely additions                                                                    |
| ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| Config and CLI                 | `src/config/config-manager.ts`, `src/cli/index.ts`, `src/cli/commands/config.ts`                                              | focused config-path/value helpers if they keep the manager small                    |
| Event delivery                 | `src/core/types.ts`, `src/core/daemon.ts`, `src/core/event-router.ts`, `src/notifications/notification-policy.ts`             | notification scheduler with a testable clock/timer seam                             |
| Native notifications and sound | `src/notifications/notification-service.ts`, `desktop-notification.ts`, `sound-service.ts`                                    | platform audio-volume and activation adapters as required by verified OS support    |
| Application focus              | `src/platform/platform.ts`, `src/platform/windows/index.ts`, `src/platform/macos/index.ts`, `src/sessions/session-manager.ts` | `src/applications/` service, association model, Windows/macOS focus implementations |
| Setup/status/docs              | `src/cli/commands/setup.ts`, `src/cli/status-view.ts`, `README.md`, `package.json`                                            | matching unit/integration tests                                                     |

## Continuous Work Log

- 2026-08-27: Created the v0.2 milestone file after reading `SPEC.md`; implementation has not started.
- 2026-08-27: Completed the mandated v0.1 repository, `CLAUDE.md`, architecture, and test-coverage audit. Existing Claude integration was verified through its adapter, normalization, settings, and end-to-end test coverage; the real user-level setup command was not run because it would modify the developer's Claude settings.
- 2026-08-27: Baseline verification passed: `npm test` (24 files; 314 passed, 1 skipped), `npm run typecheck`, `npm run lint`, and `npm run build`.
- 2026-08-27: Recorded the v0.2 module-level implementation and verification plan. No production code has been changed.
- 2026-08-27: Completed the hierarchical notification-configuration feature. Added v0.2 defaults, per-event enablement/delay persistence, sound enablement and validated 0–1 volume, legacy v0.1 config normalization, nested configuration reads, and `pingback config get <path>` registration.
- 2026-08-27: Updated notification policy to apply global notification/sound settings and to carry volume; all enabled v0.2 event classes now use sound by default. macOS uses `afplay -v`; Windows uses a temporary gain-scaled copy of PingBack's generated PCM WAV before native playback, without changing system volume or adding a dependency.
- 2026-08-27: Focused verification passed (75 tests), followed by the full suite (321 passed, 1 skipped), lint, typecheck, build, and formatting checks. The configured event delay is persisted in this feature; delayed dispatch is the next implementation feature.
- 2026-08-27: Completed delayed notification delivery. Added a cancellable, session-scoped scheduler; blocking attention is delivered immediately, while configured question/error/task delays are acknowledged over IPC without blocking Claude hooks. Returning a session to `working` or ending it cancels its pending delivery.
- 2026-08-27: Delayed-delivery verification passed: standalone scheduler, policy, daemon, and real IPC end-to-end tests. Full verification passed with 327 tests (1 skipped), lint, typecheck, and build.
- 2026-08-27: Verified the current official Claude Code hooks reference before adapter changes. Added the documented `elicitation_url_dialog` and quota-resume notification variants; blocking permission, agent-input, and elicitation prompts normalize to immediate attention, `idle_prompt` normalizes to delayed turn completion, and `agent_completed` remains task completion.
- 2026-08-27: Extended the core event model with `turn_completion`, preserving distinct status, priority, delay, and sound behavior. Full test verification passed with 328 tests (1 skipped); lint, typecheck, build, and formatting remain required before commit.
- 2026-08-27: Added the platform-independent application-focus boundary. It associates an editor only when that editor explicitly reports the session project path, performs no association for ambiguous/missing projects, and treats discovery/focus failures as a safe no-op. Platform-specific discovery and foregrounding implementations are next.
- 2026-08-27: Completed project-specific IDE/application activation. On Windows, PingBack snapshots `Win32_Process` with PowerShell CIM and foregrounds the exact matched VS Code/Cursor process with `SetForegroundWindow`; on macOS, it parses an on-demand `ps` snapshot and asks System Events to foreground the exact matching process. Process discovery occurs only when an attention notification is prepared, requires explicit project-path command-line evidence, and never targets terminals or an unrelated running editor.
- 2026-08-27: Wired the focus service through daemon construction and notification activation. Activation remains best-effort, and native toast dispatch no longer waits for a user click before the daemon acknowledges/delivers the notification. Focused Windows/macOS parser, focus, association, daemon, and notification tests pass; full feature gate is running before commit.
- 2026-08-27: Improved session/application validation and CLI feedback. Multi-session tests now verify three concurrent project paths retain distinct application targets while unmatched projects never receive one. `pingback status` includes a concise active-session total alongside its attention summary. `pingback setup` reports desktop-notification availability, sound availability, and non-fatal detection of running VS Code/Cursor instances; editor association still requires project-path evidence at notification time.
- 2026-08-27: Updated release metadata to `0.2.0` and refreshed the README with v0.2 configuration defaults, event delays, sound volume, project-specific VS Code/Cursor focusing, privacy, troubleshooting, and terminal-focus limitations.
- 2026-08-27: Manual Windows validation passed for the built CLI (`pingback --version` reported `0.2.0`) and `pingback config get notifications` returned the documented hierarchical defaults. The live project-specific editor probe returned no match for `C:\\PingBack` because no supported editor process explicitly reported that project, confirming the safe no-guess fallback. Native focus success and notification-click foregrounding require a matching editor session for a final Windows run; macOS native notification/focus validation remains required on an actual Mac and is not claimed by the automated tests.
- 2026-08-27: Final automated release gate passed at v0.2.0: 33 test files, 346 passing tests, 1 intentional skip; lint, typecheck, production build, Prettier, and `git diff --check` all passed. Native macOS focus/click validation remains an explicit real-device follow-up.
- 2026-08-27: Corrected the package-smoke configuration round-trip from the retired v0.1 `notifications.sound` key to v0.2 `notifications.sound.enabled`. The full local gate and an isolated built-CLI round-trip passed; CI must be rerun on its Windows/macOS package matrix after this correction is pushed.
- 2026-09-02: Refined notification timing and configuration (v0.2.1): added `attention_required` to `notifications.events` with a 5-second default delay (instead of hardcoded 0s immediate) to prevent overwhelming the developer with immediate sound/toast on permission or input requests, allowing sufficient time to respond directly in terminal before alerting. Updated default delays for `turn_completion` and `error` from 3s to 5s, creating a uniform 5-second default across all event types (`attention_required`, `question`, `turn_completion`, `error`, `task_completed`). Added automatic migration for legacy 3s defaults to 5s. Zero-delay (`delaySeconds: 0`) continues to trigger immediate delivery without scheduling. Bumped version to `0.2.1` and verified full test suite (33 test files, 349 passing tests, 1 intentional skip), lint, typecheck, production build, and Prettier formatting.
- 2026-09-02: Fixed IDE window focusing on notification activation: added `src/applications/editor-storage.ts` to inspect open workspace metadata (`storage.json`) for running Cursor and VS Code instances when process command lines do not expose project paths (e.g. GUI/restore launches). Added editor CLI launcher fallback (`cursor` / `code`) on Windows when native `SetForegroundWindow` encounters Electron's multi-threaded window model (`MainWindowHandle: 0`). Live Windows validation verified that `C:\PingBack` correctly matches running Cursor and foregrounds the window upon notification click. Verified full test gate with 34 test files, 356 passing tests, 1 intentional skip.
- 2026-09-02: Conducted comprehensive human-like manual testing across all v0.2 features on Windows: (1) CLI sanity and version reporting (`0.2.1`), (2) Hierarchical configuration get/set across subtrees, objects, and scalar keys, (3) Strict configuration validation and fail-fast bounds checking (volume range 0-1, non-negative delays, valid booleans) with configuration file integrity verification, (4) Daemon lifecycle (`start`, `status`, `stop`) via Windows named pipe (`\\.\pipe\pingback-<uid>`), (5) Live event ingestion via Claude Code hook entry (`dist/agents/claude/hook-entry.js`) and direct IPC across all 5 event types (`attention_required`, `question`, `turn_completion`, `error`, `task_completed`), verifying configurable 5s delays, immediate delivery with `delaySeconds: 0`, and clean cancellation upon developer prompt submission (`UserPromptSubmit`), (6) Native toast notifications and audio playback via PowerShell SoundPlayer including gain scaling and silent mode (`notifications.sound.enabled: false`), (7) Multi-session concurrent tracking across distinct projects (`PingBack`, `FinBot`, `RAGProject`) with independent waiting duration and active/attention counts in `pingback status`, and (8) Safe IDE window discovery and foreground focusing for `C:\PingBack` associating the running Cursor editor instance while safely returning no match for unknown projects.
- 2026-09-02: At the developer's request, hard-reset the branch from `9439ba5` to `77c895f`, removing the two subsequent AGY commits. Audited the reset baseline and retained the user-approved five-second defaults for all five configured notification event types. Identified the remaining Windows activation defect: `node-notifier` supplies SnoreToast activation data as its second callback argument object, while PingBack only recognized the string `"activate"`; tests will now be written against that real callback contract before the implementation is changed.
- 2026-09-02: Repaired native notification activation test-first. PingBack now recognizes Windows SnoreToast action-result objects and macOS action metadata, and supplies `Open Project` only when a project-associated focus handler exists. Verified both tests failed against the previous implementation and pass after the fix. Also removed the lossy migration that rewrote explicitly configured 3-second completion/error delays to 5 seconds: five seconds remains the default for every event, while a user's valid override now persists. Final verification passed: 34 test files, 359 passing tests, 1 intentional skip; typecheck, lint, Prettier, production build, and `git diff --check`. The built CLI reports v0.2.1 and confirms all five live notification delays are 5 seconds; the daemon remains stopped.
- 2026-09-02: Added stale-hook cleanup to Claude setup: it removes retired PingBack handlers from old hook events while retaining unrelated handlers, then installs the current documented `Notification`, `StopFailure`, `SessionStart`, `UserPromptSubmit`, and `SessionEnd` hooks. The regression test was red before the change and green afterward; the final release gate passed with 34 test files, 360 passing tests, and 1 intentional skip, plus typecheck, lint, Prettier, and build. Ran setup on this Windows installation, verified the active hook list has no `Stop`, restarted the daemon from the repaired build, and verified Claude Code is connected. Removed 17 clearly named AGY synthetic session records after backing up `sessions.json` as `sessions.json.pingback-pre-cleanup-backup`; four UUID sessions were deliberately retained.
- 2026-09-02: Diagnosed a Windows toast-delivery failure reported during real Claude use: sound played and the daemon logged delivery, but `node-notifier` translated PingBack's custom `appName` into an unregistered SnoreToast App ID, which modern Windows can suppress. Added a red/green regression test and now omit the custom identity on Windows so SnoreToast uses its registered Start-menu identity; macOS retains the PingBack app name. Rebuilt and restarted the daemon, dispatched a silent production-path diagnostic toast, and verified the SnoreToast registration exists. Full regression suite passed: 34 test files, 361 passing tests, 1 intentional skip; typecheck, lint, Prettier, and production build also passed. Physical banner visibility still requires the developer to observe the diagnostic on the active Windows desktop.
- 2026-09-02: Corrected the preceding root-cause assumption after an end-to-end source and Windows audit. The real Claude event reached the daemon, invoked `node-notifier`, and Windows recorded it under the PingBack notification identity at the exact delivery time; a no-App-ID diagnostic was also accepted under SnoreToast. The earlier identity removal was therefore not a proven fix and was reverted. Restored the stable v0.1 PingBack identity on every supported platform with a red/green regression test. Native banner presentation is now explicitly distinguished from accepted toast delivery; it remains subject to the user's Windows notification/Do Not Disturb policy and cannot be truthfully inferred from the daemon log alone.
- 2026-09-02: Repaired notification recovery and diagnostics after a live report of missing banners. The Claude hook bridge previously dropped an attention event whenever the local daemon was stopped; it now makes one bounded three-second daemon-start attempt and retries that same event without blocking Claude or writing to its terminal. Extracted detached daemon startup into core so both `pingback start` and hook recovery use the same lifecycle behavior. Windows SnoreToast `hidden` outcomes now explicitly report Windows suppression, while dismissed and timed-out notifications are reported separately; core logs say `notification dispatched` until the native notifier reports its outcome. Test-first coverage was added for recovery, native-toast outcomes, and truthful daemon logging.
- 2026-09-02: Isolated the live Windows banner failure to an unregistered custom toast App ID. Windows accepted notifications, but PingBack used the `PingBack` App ID without a matching Start-menu registration; actionable PingBack toasts returned `hidden`. The registered default SnoreToast identity displayed an action and returned `Open Project`, proving the diagnosis. Added platform-isolated, best-effort installation of PingBack's Start-menu/App-ID registration through the bundled SnoreToast binary before every Windows daemon launch. The registration is non-fatal but logged if unavailable, and is automatically applied after upgrades or daemon recovery. Final verification passed: 36 test files, 366 passing tests, 1 intentional skip; typecheck, lint, production build, Prettier, and `git diff --check` passed. A fresh registered live-toast verification follows after daemon restart.
- 2026-09-02: Completed live Windows verification after registration. The repaired daemon installed `PingBack` as a Start-menu App ID, accepted a final project-associated attention event, and Windows returned `timedout` rather than `hidden`, confirming it no longer suppressed the registered toast. The daemon remains running and Claude-connected. Removed only the three temporary verification sessions after backing up `sessions.json` as `sessions.json.pingback-verification-cleanup-backup-20260902`; the existing seven real sessions were preserved.
- 2026-09-03: Narrowed project activation to the supported editor set requested by the developer: Cursor and Visual Studio Code only. Test-first repair rejects arbitrary application IDs at the Windows boundary, refreshes the project-matched editor at notification-click time rather than using a stale process snapshot, and logs native activation receipt plus the resulting focus outcome without logging project contents. The red/green focused tests, typecheck, lint, Prettier, and production build pass. A real registered Windows toast click remains the final verification step.
- 2026-09-03: Restarted the built daemon and dispatched two registered, project-associated Cursor verification toasts. Windows accepted both and reported `timedout`; neither was clicked, so a physical toast-click foreground result is not claimed. The direct Windows Cursor focus operation remains successful, while automated coverage verifies the native SnoreToast `Open Project` action reaches the refreshed Cursor/VS Code focus callback. Removed exactly the two temporary verification sessions after backing up `sessions.json` as `sessions.json.pingback-cursor-verification-cleanup-backup-20260903`. Final regression suite passed: 36 files, 369 tests, 1 intentional skip; Prettier and `git diff --check` passed. The daemon is running and Claude-connected with the original eight sessions preserved.
- 2026-09-03: Corrected a live Windows foreground false positive: a notification click reached Cursor but only highlighted its taskbar window because PingBack treated `SetForegroundWindow`/editor-launch success as proof of focus. Windows focus now restores and requests foreground, verifies the foreground window owner, and retries through a temporary input-queue attachment before reporting success. Cursor/VS Code launch fallback must now pass the same owner verification. The fix was test-first; focused platform tests expanded from 5 to 6 cases and the full suite passed with 370 tests and 1 intentional skip. Dedicated Claude hook recovery/settings plus end-to-end scenarios passed (32 tests), confirming no hook configuration or delivery behavior changed. A real registered toast click then produced activation and verified Cursor as the actual foreground owner. Removed only that temporary session after backing up `sessions.json` as `sessions.json.pingback-foreground-verification-cleanup-backup-20260903`, then restarted the daemon.
- 2026-09-03: Confirmed commit `9631bf7` passed GitHub CI across Windows and macOS on Node 20, 22, and 24, including package-smoke jobs. Implemented macOS foregrounding parity test-first: the System Events adapter now accepts only Cursor and VS Code, activates the project-matched editor, verifies that exact process is actually frontmost, and retries once before reporting failure. The new tests covered a successful verified foreground result, an editor left behind another app, and an unsupported application ID; they failed before the implementation and pass afterward. Final local gate passed: 36 test files, 372 passing tests, 1 intentional skip; Prettier, lint, typecheck, and production build also passed. Physical notification-click foregrounding remains to be exercised on a macOS desktop with System Events accessibility permission.
